# pribate_agent — 業務エージェントチーム

## プロジェクト概要

業務全般のタスクを Claude Code 上で処理するためのサブエージェント群。
オーケストレーター集中型で、メイン Claude が `.claude/agents/` の専門エージェントへ
タスクを振り分ける。

## エージェント一覧

| 名前 | 担当領域 |
|---|---|
| `mail` | Outlook メール: 確認・要約・返信ドラフト |
| `expense` | 精算: 領収書整理・分類・申請ドラフト |
| `plan-doc` | 企画書・提案書の執筆 |
| `brainstorm` | アイデアの発散と収束 |
| `market-research` | 市場・競合・トレンド調査 |
| `task-controller` | 全体タスク管理・日次ブリーフ |

詳細は `docs/design.md` 参照。

## 振り分けの基本方針

ユーザーからの依頼を受けたら、メイン Claude (オーケストレーター) は次のように動く:

1. タスクを分解し、依存関係を判定
2. 独立タスクは並列でサブエージェントへ委譲
3. 依存タスクは順次実行 (例: market-research → brainstorm → plan-doc)
4. 中間成果物は `workflows/<run-id>/` に Markdown で保存し、ファイルパスで受け渡す
5. 最終結果を統合してユーザーへ提示

## 不可逆操作の承認ゲート

以下は**必ずユーザー承認**を経てから実行する:

- メール送信
- 精算申請の最終提出
- 外部共有 (Teams 投稿、CC 追加 等)
- カレンダー招待の確定送信

サブエージェントはドラフト生成までを担当し、送信・提出は行わない。

## 共通の作法

- 言語: 日本語 (です・ます調)。引用・固有名詞は原語のまま。
- 出典: 外部情報は URL / ファイルパスとアクセス日を併記。
- 個人情報・金額: 中間ファイルでは `workflows/` 以下に置き、`.gitignore` で除外。
- 機密情報をリポジトリへコミットしない。

## ディレクトリ

```
.claude/agents/   サブエージェント定義
.claude/commands/ 定型ワークフローのスラッシュコマンド
docs/             設計ドキュメント
templates/        企画書・精算書テンプレート
workflows/        実行時の中間成果物 (gitignore)
```

## 開発ブランチ

Claude Code on the web からの作業は `claude/agent-team-business-tasks-ZqYPH` ブランチで行う。
